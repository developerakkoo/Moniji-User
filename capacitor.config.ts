import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.moniji.starter',
  appName: 'Moniji',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
